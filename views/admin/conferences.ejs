<%- include("../partials/header.ejs") %> <%- include("../partials/navbar.ejs")
%>
<div class="col-xxl-8 py-5 mycont">
  <div class="column justify-content-center">
    <div class="d-flex column justify-content-center gap-2">
      <div class="d-flex gap-2 justify-content-center">
        <form action="/getAllConferences" method="post">
          <button
            class="btn btn-primary d-inline-flex align-items-center justify-content-center"
            type="submit"
          >
            Conferences
          </button>
        </form>
      </div>

      <div class="d-flex gap-2 justify-content-center">
        <form action="/getSpecificConference" method="post">
          <button
            class="btn btn-primary d-inline-flex align-items-center justify-content-center"
            type="submit"
          >
            Conference
          </button>
        </form>
      </div>
    </div>
    <div class="video mt-5">
      <video src="videos/finally.mp4" controls></video>
    </div>
  </div>
</div>
<%- include("../partials/footer.ejs") %>
